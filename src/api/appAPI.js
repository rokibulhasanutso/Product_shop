const rootAPIUrl = 'https://dummyjson.com'

const productUrl = `${rootAPIUrl}/products`

const productCategoriUrl = `${rootAPIUrl}/products/categories`

export { rootAPIUrl, productUrl, productCategoriUrl }